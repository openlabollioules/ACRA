{
  "dependencies": {
    "pptx-automizer": "^0.6.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  },
  "scripts": {
    "build": "esbuild src/services/merge_pptx.js --bundle --platform=node --outfile=dist/bundle.js"
  }
}
